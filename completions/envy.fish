complete -c envy -e
complete -c envy -f
complete -c envy -a init -d "Create default compose file"
complete -c envy -a switch -d "Recreate containers"
complete -c envy -a up -d "Start containers"
complete -c envy -a down -d "Stop containers"
